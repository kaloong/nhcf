TODO:
-----
Detect what delimiter is should use. DONE
Detect duplicates in CSV file. 
Log all message and errors generated by the script. 
Create cfg parser to store options for processing.
Implement transportation of host/hostgroup folders to Nagios host?
Prettify the generated host/hostgroup files e.g. consistent tabbing alignment. DONE 
Review internal logic and see if the design can use different data structure other than dict. E.g. PriorityQueue
